import { NextResponse } from "next/server";import { KnowledgeService } from "@/lib/platform/knowledge-service";import { logUnauthenticatedAction } from "@/lib/platform/audit-log";const service=new KnowledgeService();
export async function GET(request:Request){const q=new URL(request.url).searchParams.get("q");return NextResponse.json(q?{results:service.search(q)}:{items:service.list()})}
export async function POST(request:Request){const body=await request.json().catch(()=>null) as {action?:string;title?:string;category?:string;id?:string}|null;if(body?.action==="ingest"&&body.title){const created=service.ingest(body.title,body.category??"Geral");await logUnauthenticatedAction({action:"knowledge.ingest",entity:`document:${created.id}`,result:"success",reason:`Ingestão de rascunho: "${body.title}"`});return NextResponse.json(created,{status:201});}if(body?.action==="publish"&&body.id){const updated=service.publish(body.id);await logUnauthenticatedAction({action:"knowledge.publish",entity:`document:${body.id}`,result:updated?"success":"not_found",reason:"Publicação de documento de conhecimento"});return NextResponse.json(updated??{error:"Não encontrado"});}return NextResponse.json({error:"Ação inválida"},{status:400})}
